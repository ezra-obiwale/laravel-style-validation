# for local development
services:
  validation:
    image: dscribe/basic-node
    build:
      context: .
      target: base
    volumes:
      - ".:/app"
      - ~/.npmrc:/root/.npmrc:ro
    tty: true
    stdin_open: true
